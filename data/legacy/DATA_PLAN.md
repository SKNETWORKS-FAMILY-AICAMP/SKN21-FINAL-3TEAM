# 데이터 수집 계획서 (v6 — 서비스 흐름 + 동적 템플릿 필드 방식)

> 최종 수정: 2026-02-10

---

## 0. 기능별 실제 서비스 흐름

각 기능이 실서비스에서 어떻게 작동하는지, 데이터가 어디에 쓰이는지 설명합니다.

### 회의록 페이지 (meeting_generate)

```
사용자: 회의 내용을 텍스트로 입력
  → Intent Classifier: "meeting_generate" 분류
  → document_agent:
      1) 사용자 입력 텍스트를 sLLM(LoRA v2)에 전달
      2) sLLM이 구조화된 JSON 추출 (회의정보, 결정사항, action_items, 참석자)
      3) [선택] RAG로 관련 규정 검색 → 리스크 감지
      4) JSON 결과를 MeetingMinutesTemplate에 전달 → 마크다운 렌더링
      5) 미리보기 표시 → DOCX/PDF 다운로드
```

**사용 데이터:** 회의록 분석 800건 (sLLM이 원문→구조화 JSON 추출하는 능력 학습)

### 문서 요약 (doc_generate — 요약 모드)

```
사용자: 문서 파일 업로드 (PDF/DOCX) 또는 문서관리에서 선택 + "요약해줘"
  → Intent Classifier: "doc_generate" 분류
  → document_agent:
      1) 시스템이 PDF/DOCX에서 텍스트 추출 (PyMuPDF, python-docx)
      2) 문서가 길면 청크 분할 (sLLM 컨텍스트 제한 대응)
      3) 코드가 요약 필드 목록을 input에 삽입 (예: title, summary, key_points, conclusion)
      4) 추출된 텍스트 + 필드 목록을 sLLM(LoRA v2)에 전달
      5) sLLM이 필드별 요약 JSON 생성
      6) Template에 전달 → 마크다운 렌더링
      7) 미리보기 표시 → DOCX/PDF 다운로드
```

**사용 데이터:** 문서 요약 300건 (sLLM이 긴 문서→필드별 요약하는 능력 학습)
**추가 필요 코드:** 텍스트 추출기 (PDF/DOCX → 텍스트), 긴 문서 청크 분할 로직

### 문서 생성 (doc_generate — 생성 모드)

```
사용자: 요구사항 입력 + 템플릿 선택 (예: "프론트엔드 채용공고 만들어줘")
  → Intent Classifier: "doc_generate" 분류
  → document_agent:
      1) 선택된 템플릿의 필드 목록을 자동으로 가져옴
      2) 요구사항 + 필드 목록을 sLLM(LoRA v2)에 전달
      3) sLLM이 필드별 내용 JSON 생성
      4) Template에 전달 → 마크다운 렌더링
      5) 미리보기 표시 → DOCX/PDF 다운로드
```

**사용 데이터:** 문서 생성 200건 (sLLM이 요구사항→필드별 내용 생성하는 능력 학습)

### 문서 검색 (doc_search)

```
사용자: "접근 권한 관련 규정 뭐 있어?"
  → Intent Classifier: "doc_search" 분류
  → document_agent:
      1) Qdrant에서 관련 문서/규정 검색 (BM25 + Vector)
      2) Reranker로 상위 결과 정렬
      3) 검색 결과 + 사용자 질문을 sLLM(LoRA v2)에 전달
      4) sLLM이 검색 결과를 자연스럽게 정리하여 답변
```

**사용 데이터:** 문서 검색 답변 200건 (sLLM이 검색결과→정리된 답변 생성하는 능력 학습)

### 규정 판단 (judgment)

```
사용자: "인턴에게 서버 접근 권한 줘도 돼?"
  → Intent Classifier: "judgment" 분류
  → judgment_agent:
      1) Qdrant에서 관련 규정 검색 (RAG)
      2) 규정 원문 + 사용자 질문을 sLLM(LoRA v1)에 전달
      3) sLLM이 Yes/No/조건부 판단 + 근거 + 대안 생성
```

**사용 데이터:** 규정 판단 1,000건 + 규정 해석 Q&A 500건

### 리스크 감지 (회의록/문서 분석 시 자동 실행)

```
회의록 분석 또는 문서 요약 완료 후 자동 트리거:
  → 분석된 내용에서 키워드 추출
  → Qdrant에서 관련 규정 검색 (RAG)
  → 규정 원문 + 분석 내용을 sLLM(LoRA v2)에 전달
  → sLLM이 위반 여부 판단 (risk_detected, risk_level, violation 등)
  → 위반 감지 시 사용자에게 경고 표시
```

**사용 데이터:** 리스크 감지 200건

### 문서관리 (CRUD)

```
사용자: 문서 목록 조회 / 검색 / 수정 / 삭제
  → 백엔드 API (FastAPI) + DB (PostgreSQL)
  → AI 개입 없음
```

**사용 데이터:** 없음 (순수 백엔드/프론트엔드 기능)

### 추가 필요 시스템 구성요소

| 구성요소 | 설명 | 용도 |
|---------|------|------|
| **텍스트 추출기** | PDF → PyMuPDF, DOCX → python-docx | 문서 요약 시 파일에서 텍스트 추출 |
| **청크 분할기** | 긴 문서를 sLLM 컨텍스트에 맞게 분할 | 문서가 토큰 제한 초과 시 대응 |
| **Qdrant** | 규정/문서 벡터 저장소 | doc_search, judgment, 리스크 감지에 RAG 제공 |
| **Reranker** | 검색 결과 재정렬 | doc_search 정확도 향상 |

---

## 1. 학습시킬 모델 3개

| # | 모델 | 역할 | 사용하는 Agent |
|:-:|------|------|--------------|
| 1 | **Intent Classifier** (klue/bert-base) | 사용자 입력 → 7개 intent 분류 | orchestrator |
| 2 | **LoRA v1** (Qwen3-8B 등) | 규정 판단 + 규정 해석 Q&A | judgment_agent |
| 3 | **LoRA v2** (Qwen3-8B 등) | 회의록 분석 + 문서 요약 + 문서 생성 + 문서 검색 답변 + 리스크 감지 | document_agent |

---

## 2. 확보 완료 데이터

| 데이터 | 건수 | 위치 | 변환 필요 사항 |
|--------|:----:|------|--------------|
| 규정 판단 | **1,000** | `docs/판단예시_1000_듀듀테크놀로지.xlsx` | ⚠️ JSONL 변환 + input에 규정 원문 추가 (QA #1) |
| 회의록 분석 | **800** | `data/proceedings/*.json` | 포맷 OK, `"일시"` 필드 추가 완료 |
| **소계** | **1,800** | | |

### ⚠️ 판단 데이터 변환 시 주의 (QA #1)

현재 Excel input:
```
"배수지(신입사원)이 퇴직자의 접근 권한을 즉시 회수하려 합니다. 맞나요?"
```

변환 후 input (규정 원문 포함해야 함):
```
"정보보안 규정 제17조 제4항: 인사 변동(퇴직) 발생 시 즉시 권한 변경·회수해야 한다.\n\n질문: 배수지(신입사원)이 퇴직자의 접근 권한을 즉시 회수하려 합니다. 맞나요?"
```

> 실서비스에서 RAG가 규정 텍스트를 붙여서 모델에 전달하므로, 학습 데이터도 동일한 형태여야 함

### 회의록 분석 output 스키마 (확정)

```json
{
  "회의정보": {
    "제목": "인턴 프로덕션 DB 접근 요청 논의",
    "유형": "권한 변경 회의",
    "핵심주제": "인턴의 프로덕션 DB 접근 권한 요청 처리",
    "일시": "2026-02-10 14:00"
  },
  "결정사항": ["인턴 조현빈의 프로덕션 DB 접근 불허", "테스트 DB에 최근 덤프를 올려서 디버깅 진행"],
  "action_items": [
    { "담당자": "노태경", "내용": "테스트 DB에 프로덕션 덤프 세팅", "기한": "내일", "우선순위": "높음" }
  ],
  "참석자": [
    { "이름": "서지안", "직책": "CTO" }
  ]
}
```

> `"일시"` 필드: input에서 날짜/시간 추출. 없으면 빈 문자열 → 프론트엔드에서 오늘 날짜로 기본값 처리

---

## 3. 신규 수집 데이터 — 전체 설계

### 핵심 설계: 동적 템플릿 필드 방식

문서 요약/생성 데이터는 **input에 템플릿 필드 목록을 포함**시켜서, sLLM이 어떤 템플릿이든 대응할 수 있도록 학습시킵니다.

```
사용자 요청 → 코드가 템플릿 필드 목록을 input에 삽입 → sLLM이 필드별 내용 생성 → Template이 렌더링
```

이 방식의 장점:
- 시스템 템플릿 4개 + 사용자 커스텀 템플릿 모두 동일하게 처리
- 새 템플릿 추가 시 데이터 재수집 불필요
- Template 코드와 자연스럽게 연동

---

### 3-1. Intent Classifier — 1,400건

| intent | 목표 | 경계 케이스 포함 (QA #5) |
|--------|:----:|------------------------|
| `judgment` | 200 | "연차 규정에 따라 내일 쉬어도 돼?" (judgment vs doc_search) |
| `doc_search` | 200 | "보안 규정 위반한 것 같은데 확인해줘" (doc_search vs judgment) |
| `doc_generate` | 200 | "이 보고서 요약하고 새로 써줘" (summary vs generate) |
| `meeting_generate` | 200 | "회의록 정리하고 일정도 잡아줘" (meeting vs schedule) |
| `schedule_add` | 200 | "다음 주 미팅 잡아줘" (add vs view 구분) |
| `schedule_view` | 200 | "내일 뭐 있어?" (view vs general) |
| `general` | 200 | "고마워, 근데 연차 며칠 남았어?" (general → doc_search 전환) |
| **합계** | **1,400** | 카테고리별 20~30건은 경계 케이스로 |

**양식:**
```jsonl
{"text": "인턴에게 AWS 접근 권한 줘도 되나요?", "label": "judgment"}
{"text": "연차 규정에 따라 내일 쉬어도 돼?", "label": "judgment"}
```

---

### 3-2. LoRA v1 — 규정 해석 Q&A — 500건 (신규)

> 판단 1,000건은 이미 확보. Q&A 500건만 추가 수집.

```json
{
  "instruction": "다음 규정 관련 질문에 대해 상세하게 답변해주세요.",
  "input": "정보보안 규정 제3조: 최소 권한 원칙에 따라 접근권한을 부여한다.\n\n질문: 최소 권한 원칙이 뭔가요?",
  "output": "최소 권한 원칙은 직원에게 업무 수행에 필요한 최소한의 접근 권한만 부여하는 것입니다.\n\n예를 들어:\n- 개발자라도 담당하지 않는 프로젝트 DB에는 접근 불가\n- 읽기만 필요하면 SELECT 권한만 부여\n\n권한 부여 절차: 부서장 신청 → 보안팀 검토 → 승인 후 IT팀 설정"
}
```

- 듀듀테크놀로지 규정 기반 (판단 데이터와 동일 규정)
- ⚠️ input에 반드시 규정 원문 포함 (QA #1 동일)

---

### 3-3. LoRA v2 — document_agent 전용 — 900건 (신규)

#### A. 문서 검색 답변 (doc_search) — 200건 (QA #2 해결)

```json
{
  "instruction": "다음 검색 결과를 바탕으로 사용자 질문에 답변하세요.",
  "input": "검색 결과:\n1. 정보보안 규정 제3조: 최소 권한 원칙에 따라 접근권한 부여\n2. 인사 규정 제15조: 인턴 및 계약직 시스템 접근 제한\n\n질문: 접근 권한 관련 규정이 뭐가 있어?",
  "output": "접근 권한과 관련된 규정은 크게 두 가지입니다:\n\n1. **정보보안 규정 제3조** — 최소 권한 원칙에 따라 업무에 필요한 최소한의 권한만 부여\n2. **인사 규정 제15조** — 인턴 및 계약직은 정규직보다 제한된 접근 권한 적용\n\n자세한 내용이 필요하시면 말씀해주세요."
}
```

#### B. 문서 요약 (동적 필드 방식) — 300건

sLLM이 **input에 포함된 필드 목록**을 읽고 해당 필드를 채워서 요약합니다.

```json
{
  "instruction": "주어진 필드에 맞게 문서를 요약하세요.",
  "input": "필드: title, summary, key_points, conclusion\n\n원본 문서:\n제목: 2026년 1분기 보안 감사 보고서\n\n1. 감사 개요\n감사 기간: 2026.01.06 ~ 2026.01.17\n감사 대상: 전사 IT 인프라\n\n2. 주요 발견사항\n- 퇴사자 계정 미삭제 12건\n- 외부 접근 차단 정상 작동\n- 레거시 DB 암호화 미적용 3건\n\n3. 종합 평가\n전반적 보안 수준 B+ 등급으로 양호",
  "output": {
    "title": "2026년 1분기 보안 감사 보고서",
    "summary": "전반적 보안 수준 B+ 평가. 퇴사자 계정 미삭제, 레거시 DB 암호화 미적용 등 일부 개선 필요",
    "key_points": "- 퇴사자 계정 미삭제 12건 발견\n- 외부 침입 시도 전부 차단 성공\n- 레거시 DB 암호화 미적용 3건",
    "conclusion": "계정 삭제 자동화 시스템 도입 및 레거시 DB 암호화 작업 필요"
  }
}
```

**다양한 필드 조합으로 학습시켜야 함:**

| 필드 조합 예시 | 건수 |
|--------------|:----:|
| `title, summary, key_points, conclusion` (보고서형) | 80 |
| `title, summary, key_points, action_required` (감사/점검형) | 60 |
| `title, background, summary, key_points` (기획서형) | 60 |
| `title, summary, recipients, action_required` (공지/메일형) | 50 |
| `title, summary, key_points, technical_details` (기술문서형) | 50 |

> **핵심:** 필드 조합이 다양해야 모델이 어떤 필드 목록이 와도 대응 가능

#### C. 문서 생성 (동적 필드 방식) — 200건

sLLM이 **input에 포함된 템플릿 필드 목록**을 읽고 각 필드를 채워서 문서를 생성합니다.

**C-1. 짧은 요구사항 → 필드별 문서 생성 (100건)**

```json
{
  "instruction": "주어진 템플릿 필드에 맞게 각 항목을 작성하세요.",
  "input": "템플릿: 채용공고\n필드: position, employment_type, experience, responsibilities, requirements, preferred, benefits\n\n요구사항: 프론트엔드 개발자, 2년차, React, 재택 가능",
  "output": {
    "position": "프론트엔드 개발자",
    "employment_type": "정규직",
    "experience": "2년 이상",
    "responsibilities": "- React 기반 웹 서비스 개발 및 유지보수\n- UI/UX 개선 및 성능 최적화\n- 백엔드 API 연동",
    "requirements": "- React 실무 경력 2년 이상\n- JavaScript/TypeScript 능숙\n- Git 기반 협업 경험",
    "preferred": "- Next.js 경험\n- 디자인 시스템 구축 경험\n- CI/CD 파이프라인 이해",
    "benefits": "- 재택근무 주 2회\n- 자기개발비 연 100만원\n- 유연근무제"
  }
}
```

**C-2. 긴 원본 → 템플릿 필드별 재구성 (100건)**

```json
{
  "instruction": "주어진 템플릿 필드에 맞게 문서를 재구성하세요.",
  "input": "템플릿: 보고서\n필드: title, date, author, summary, content, conclusion\n\n원본 문서:\n지난주 개발팀에서 보안 감사를 진행했습니다. 총 42개 항목을 점검했으며 3건의 보안 취약점이 발견되어 긴급 패치를 완료했습니다. 다음 분기에는 자동화 도구를 도입할 예정입니다.",
  "output": {
    "title": "보안 감사 결과 보고서",
    "date": "",
    "author": "",
    "summary": "전사 보안 감사 42개 항목 점검, 취약점 3건 발견 및 긴급 패치 완료",
    "content": "## 점검 결과\n- 점검 항목: 42개\n- 발견 취약점: 3건\n- 조치: 긴급 패치 완료\n\n## 향후 계획\n- 다음 분기 자동화 도구 도입 예정",
    "conclusion": "현재 보안 이슈는 모두 해결됨. 자동화 도구 도입으로 지속적 모니터링 체계 구축 필요"
  }
}
```

**4개 시스템 템플릿 필드 참고:**

| 템플릿 | 필드 목록 | C-1 | C-2 |
|--------|----------|:---:|:---:|
| 회의록 | title, date, attendees, agenda, content, decisions, action_items | 25 | 25 |
| 보고서 | title, date, author, summary, content, conclusion | 25 | 25 |
| 채용공고 | position, employment_type, experience, responsibilities, requirements, preferred, benefits | 25 | 25 |
| 제안서 | title, background, content, expected_results, timeline, resources | 25 | 25 |

> **커스텀 템플릿 대응:** 위 4개 외에도 다양한 필드 조합을 섞어서 학습시키면 사용자가 올린 커스텀 템플릿에도 대응 가능

#### D. 리스크 감지 — 200건 (QA #3 해결)

회의록/문서에서 규정 위반 가능성을 탐지:

```json
{
  "instruction": "다음 내용에서 규정 위반 여부를 감지하세요.",
  "input": "회의 내용: 인턴 박OO에게 프로덕션 서버 SSH 키를 발급하기로 결정\n\n관련 규정:\n정보보안 규정 제18조 2항 - 수습 기간 직원의 프로덕션 DB 접근 금지",
  "output": {
    "risk_detected": true,
    "risk_level": "high",
    "violation": "정보보안 규정 제18조 2항 위반",
    "detail": "수습 기간 내 직원에게 프로덕션 접근 권한 부여",
    "recommendation": "테스트 환경으로 제한, 수습 종료 후 재검토"
  }
}
```

> 회의록 800건 중 위반 시나리오를 참고하여 (위반 있음 / 없음) 균등하게 생성

---

## 4. 전체 요약표

### 확보 완료

| 데이터 | 건수 | LoRA | 할 일 |
|--------|:----:|:----:|------|
| 규정 판단 | 1,000 | v1 | JSONL 변환 + input에 규정 원문 추가 |
| 회의록 분석 | 800 | v2 | 그대로 사용 |
| **소계** | **1,800** | | |

### 신규 수집

| 데이터 | 건수 | LoRA | 우선순위 |
|--------|:----:|:----:|:--------:|
| Intent 분류 문장 | 1,400 | BERT | 높음 |
| 규정 해석 Q&A | 500 | v1 | 높음 |
| 문서 검색 답변 (doc_search) | 200 | v2 | 높음 |
| 문서 요약 (동적 필드) | 300 | v2 | 중간 |
| 문서 생성 (동적 필드) | 200 | v2 | 중간 |
| 리스크 감지 | 200 | v2 | 중간 |
| **소계** | **2,800** | | |

### 총합

```
확보 완료:    1,800건  (판단 1,000 + 회의록 800)
신규 수집:  + 2,800건
─────────────────────
총 데이터:    4,600건
```

### 모델별 학습 데이터 배분

| 모델 | 학습 데이터 | 총 건수 | 학습 85% | 평가 15% |
|------|-----------|:------:|:-------:|:-------:|
| **Intent Classifier** | intent 문장 | 1,400 | 1,190 | 210 |
| **LoRA v1** | 판단 1,000 + Q&A 500 | 1,500 | 1,275 | 225 |
| **LoRA v2** | 회의록 800 + 검색 200 + 요약 300 + 생성 200 + 리스크 200 | 1,700 | 1,445 | 255 |

---

## 5. QA 문제 반영 체크리스트

| # | QA 문제 | 해결 방법 | 반영 여부 |
|:-:|--------|----------|:--------:|
| 1 | 판단 input에 규정 원문 누락 | Excel→JSONL 변환 시 규정 원문 추가 | ✅ |
| 2 | doc_search 학습 데이터 없음 | 문서 검색 답변 200건 추가 | ✅ |
| 3 | 리스크 감지 데이터 없음 | 리스크 감지 200건 추가 | ✅ |
| 4 | LoRA v2 output 형식 혼재 | 전부 JSON output으로 통일 | ✅ |
| 5 | Intent 경계 케이스 미고려 | 카테고리별 20~30건 경계 케이스 포함 | ✅ |
| 6 | 평가 데이터 미분리 | 전체 15% 평가용 분리 | ✅ |
| 7 | Template 필드 ↔ sLLM output 불일치 | 동적 필드 방식으로 해결 (input에 필드 목록 포함) | ✅ |
| 8 | 커스텀 템플릿 대응 불가 | 동적 필드 방식으로 어떤 템플릿이든 대응 | ✅ |

---

## 6. 5명 역할 분담 (추천)

| 담당자 | 데이터 | 건수 | 참고 |
|--------|--------|:----:|------|
| **1번** | Intent 분류 (judgment + doc_search + doc_generate) | 600 | GPT/Claude 대량 생성 |
| **2번** | Intent 분류 (meeting + schedule_add + schedule_view + general) | 800 | GPT/Claude 대량 생성 |
| **3번** | 규정 해석 Q&A 500건 | 500 | 듀듀테크놀로지 규정 기반 |
| **4번** | 문서 요약 300건 + 문서 검색 답변 200건 | 500 | 다양한 필드 조합 + 문서 종류 |
| **5번** | 문서 생성 200건 + 리스크 감지 200건 | 400 | 4개 템플릿 필드 기반, 위반/정상 균등 |

**경은/승언:** 카테고리별 예시 10~20건 먼저 작성 → 나머지 팀원이 대량 생성 → 최종 검수

---

## 7. 파일 저장 위치

```
data/
├── proceedings/                              # 회의록 800건 (확보 완료, 일시 필드 추가됨)
│
├── training/
│   ├── v1_judgment/
│   │   ├── judgment_train.jsonl             # ← Excel 변환 (1,000건)
│   │   └── regulation_qa_train.jsonl        # ← 신규 (500건)
│   │
│   └── v2_document/
│       ├── meeting_train.jsonl              # ← proceedings/ 변환 (800건)
│       ├── doc_search_train.jsonl           # ← 신규 (200건) [QA #2]
│       ├── doc_summary_train.jsonl          # ← 신규 (300건) [동적 필드]
│       ├── doc_generate_train.jsonl         # ← 신규 (200건) [동적 필드]
│       └── risk_detection_train.jsonl       # ← 신규 (200건) [QA #3]
│
├── intent/
│   └── intent_train.jsonl                   # ← 신규 (1,400건)
│
└── evaluation/                              # 각 데이터의 15% 분리
    ├── v1_eval.jsonl
    ├── v2_eval.jsonl
    └── intent_eval.jsonl
```

---

## 8. train 스크립트 수정 필요

| 파일 | 현재 주석 | 실제 계획 |
|------|----------|----------|
| `train_v1_judgment.py` | 1,300개 (판단500 + Q&A800) | **1,500개 (판단1,000 + Q&A500)** |
| `train_v2_document.py` | 700개 (회의400 + 문서300) | **1,700개 (회의800 + 검색200 + 요약300 + 생성200 + 리스크200)** |

---

## 9. Template 코드 수정 필요

동적 필드 방식에 맞춰 Template 코드 수정이 필요합니다:

| 파일 | 수정 내용 |
|------|----------|
| `meeting_minutes.py` | REQUIRED_FIELDS를 데이터 스키마에 맞게 변경 (회의정보, 결정사항, action_items, 참석자) |
| `base.py` | render() 메서드가 동적 필드 dict를 받아서 마크다운으로 렌더링하도록 수정 |
| 모든 템플릿 | sLLM output (JSON dict) → TEMPLATE에 삽입하는 매핑 로직 추가 |

---

## 10. 작업 순서

```
1단계: 경은/승언이 카테고리별 예시 10~20건 작성 (기준 잡기)
2단계: 판단 Excel → JSONL 변환 스크립트 작성 (규정 원문 포함)
3단계: 5명이 역할 분담대로 데이터 수집 (GPT/Claude 활용)
4단계: 경은/승언 최종 검수 + 평가 데이터 15% 분리
5단계: Template 코드 수정 (동적 필드 대응)
6단계: 파인튜닝 시작
```

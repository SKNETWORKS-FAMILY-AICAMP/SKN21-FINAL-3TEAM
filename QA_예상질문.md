# Q&A 예상 질문 & 답변

> presentation_v4_최종.html 기준 | 2026-03-31

---

## 기술 질문 (10개)

### Q1. "왜 Kanana-1.5-8B을 선택했나요?"
"Planner 태스크에서 Qwen3-8B과 비교했는데, 유효 응답률 94.7% vs 68.4%, 추론 속도 2.3초 vs 11.8초로 Kanana가 압도적이었습니다. 한국어 특화 모델이라 규정 판단 같은 한국어 도메인에도 유리했고요."

### Q2. "Intent 분류를 LLM으로 안 한 이유는?"
"매 메시지마다 실행되는데 LLM으로 하면 1~2초 걸립니다. roberta-large 앙상블은 19ms — 100배 빠르면서 정확도 91%입니다."

### Q3. "RAG에 HyDE를 쓴 이유는?"
"사용자가 '연차 며칠?'이라고 물으면 규정엔 '유급휴가 부여 일수'라고 적혀있습니다. 단어가 달라서 검색이 안 되는데, HyDE가 LLM으로 규정 스타일로 변환해서 검색합니다."

### Q4. "RAG 학습에 넣으면 왜 오히려 성능이 떨어졌나요?"
"RAG 검색 결과에 노이즈가 섞여서 학습 데이터의 라벨과 모순이 생겼습니다. 정답은 'yes'인데 RAG가 관련 없는 조항을 가져오면 모델이 혼란을 느끼는 거죠. 그래서 학습은 정확한 규정 원문으로, 서빙할 때만 RAG를 쓰는 전략으로 바꿨습니다."

### Q5. "4중 보조장치가 응답 속도에 영향 안 주나요?"
"4단계 모두 규칙 기반 검증이라 LLM 호출이 아닙니다. 키워드 매칭, 조항 존재 확인, 카테고리 체크, 캐시 조회 — 전부 밀리초 단위라 체감 지연 없습니다."

### Q6. "Conditional 78%를 어떻게 올릴 계획인가요?"
"Conditional은 yes와 no의 경계에 있는 케이스라 라벨 자체가 모호합니다. 라벨 재정의와 경계 사례 데이터를 집중 보강해서 85%까지 올리는 게 목표입니다."

### Q7. "문서요약 파인튜닝 효과가 소폭인데, 왜 필요했나요?"
"요약 품질 자체는 Base도 괜찮았습니다. 하지만 '뉴스기사'같은 없는 카테고리를 만들어서 문서관리 페이지 필터가 깨졌습니다. 파인튜닝으로 분류 유효율을 85%에서 100%로 올려서 서비스 안정성을 확보한 겁니다."

### Q8. "커스텀 템플릿이 복잡한 표를 못 다루면?"
"현재 Placeholder 방식은 라벨-빈칸 패턴이라 단순한 표는 잘 되지만, 병합 셀이나 중첩 표는 어렵습니다. 향후 다양한 문서 양식 기반으로 학습 데이터를 확장할 계획입니다."

### Q9. "vLLM에서 LoRA 핫스왑이 실제로 되나요?"
"vLLM이 런타임에서 여러 LoRA 어댑터를 동시 로드하고, 요청에 따라 자동 전환합니다. 현재 4종 어댑터(generate, summary, judgment, planner)를 한 서버에서 운영 중입니다."

### Q10. "3-step 복합 요청이 66.7%인데, 실용성이 있나요?"
"1-step 91%, 2-step 85%로 대부분의 실무 요청은 커버됩니다. 3-step은 8B 모델의 추론 한계인데, 데이터 추가와 모델 스케일업으로 개선 계획입니다."

---

## 기획 질문 (5개)

### Q1. "왜 GPT 안 쓰고 sLLM인가요?"
"타겟이 제조·IT 중견기업인데, 대기업 납품 시 보안 서약서를 씁니다. 외부 AI에 사내 데이터 보내면 거래가 끊깁니다. sLLM은 사내 서버에서 돌리니까 데이터 유출 자체가 불가능하고, API 비용도 0원입니다."

### Q2. "Base 모델 한계 예시가 실제 돌린 건가요?"
"평가 결과를 기반으로 재구성한 예시입니다. 정확도 37.2%, False Fill 44.3%, 분류 유효율 85% — 이 수치는 eval 데이터셋으로 측정한 실제 수치입니다."

### Q3. "85.4%가 진짜 실무에서 쓸 수 있는 수준인가요?"
"85.4%만 보면 부족해 보이지만, 4중 보조장치가 있습니다. confidence가 낮으면 사용자에게 확인을 요청하고, 환각이 감지되면 신뢰도를 대폭 낮춥니다. 틀릴 때 '모르겠다'고 말할 수 있는 게 핵심입니다."

### Q4. "다른 회사에 도입하려면 데이터를 다시 만들어야 하나요?"
"Intent와 문서생성/요약 어댑터는 범용이라 재사용 가능합니다. 회사별로 다른 건 규정 DB뿐인데, 규정 문서를 Qdrant에 올리면 RAG가 알아서 검색합니다. 판단 어댑터만 해당 회사 규정으로 추가 학습하면 됩니다."

### Q5. "수익화 모델은 뭔가요?"
"초기에는 규정 DB 구축 + LoRA 학습 컨설팅으로 시작하고, 중기에는 SaaS 플랫폼으로 전환할 수 있습니다. 100명 규모 기업 기준 월 300~500만원이면 GPT Enterprise 대비 절반 이하 비용입니다."

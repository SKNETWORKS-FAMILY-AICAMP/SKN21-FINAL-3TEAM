/**
 * JsonViewer - 원본 JSON 보기 (팀원 E 담당)
 *
 * UI_UX.pdf: "[추가] '원본 JSON 보기' 버튼 (구조화된 데이터 접기/펼치기)"
 * 요구사항: FR-DOC-004
 *
 * Props:
 *   - data: JSON 데이터 (object)
 *   - title: 제목 (예: "AI 분석 원본 데이터")
 *   - defaultExpanded: 기본 펼침 상태 (기본: false)
 *
 * 기능:
 *   - 회의 상세 > AI 분석 결과 탭에서 "원본 JSON 보기" 버튼
 *   - 클릭 시 구조화된 JSON 데이터 접기/펼치기
 *   - 복사 버튼
 */
export default function JsonViewer() {
  return (
    <div>
      {/* TODO: 팀원 E 구현
        1. "원본 JSON 보기" 토글 버튼
        2. JSON 트리 뷰 (접기/펼치기)
        3. 클립보드 복사 버튼
        4. 문법 하이라이팅 (선택적)
      */}
    </div>
  )
}

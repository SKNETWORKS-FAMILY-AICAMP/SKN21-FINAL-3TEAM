/**
 * CalendarView (팀원 E 담당)
 * - 일정 목록 / FullCalendar 표시
 * - MeetLinkBadge로 Meet 링크 표시
 * - SCHEDULE_COLORS로 일정 타입 색상 구분
 */
import MeetLinkBadge from './MeetLinkBadge'

export default function CalendarView() {
  return (
    <div>
      {/* TODO: 팀원 E 구현 */}
      {/* - listSchedules() API로 일정 목록 조회 */}
      {/* - 일정별 MeetLinkBadge 표시 (schedule.google_meet_link) */}
      {/* - 우선순위 뱃지 (high/medium/low) */}
    </div>
  )
}
